{
  "name": "standard-schema",
  "version": "0.10.8",
  "description": "Runtime type validation",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "bin": {
    "standard-schema": "dist/bin/index"
  },
  "scripts": {
    "lint": "tslint \"src/**/*.ts\"",
    "build": "rimraf dist && tsc",
    "test:watch": "jest --watch",
    "test:spec": "jest --coverage",
    "test": "npm run -s lint && npm run -s build && npm run -s test:spec",
    "prepublish": "typings install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/blakeembrey/jaywalk.git"
  },
  "keywords": [
    "schema",
    "standard",
    "data",
    "json",
    "validation",
    "types",
    "runtime",
    "serialize"
  ],
  "author": {
    "name": "Blake Embrey",
    "email": "hello@blakeembrey.com",
    "url": "http://blakeembrey.me"
  },
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/blakeembrey/jaywalk/issues"
  },
  "homepage": "https://github.com/blakeembrey/jaywalk",
  "jest": {
    "transform": {
      ".(ts|tsx)": "ts-jest/preprocessor.js"
    },
    "testRegex": "src/.*\\.(?:test|spec)\\.(ts|tsx|js)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ]
  },
  "devDependencies": {
    "istanbul": "^0.4.3",
    "jest": "^18.1.0",
    "rimraf": "^2.5.4",
    "ts-jest": "^18.0.3",
    "tslint": "^4.4.2",
    "tslint-config-standard": "^4.0.0",
    "typescript": "^2.1.6",
    "typings": "^2.1.0"
  },
  "dependencies": {
    "arrify": "^1.0.1",
    "jsonld": "^0.4.11"
  }
}
